const fs = require('fs');
const path = require('path');
const p = path.join(process.cwd(), 'server.js');
let s = fs.readFileSync(p, 'utf8');
function rep(oldValue, newValue, label) {
  if (!s.includes(oldValue)) throw new Error('server trecho nao encontrado: ' + label);
  s = s.replace(oldValue, newValue);
}
rep('import { completeProject200Onboarding, ensureProject200OnboardingSchema, getProject200Onboarding, initializeProject200Onboarding, markProject200OnboardingAvatarComplete, restartProject200Onboarding, saveProject200OnboardingProgress } from "./src/project200-onboarding.js";\n', 'import { completeProject200Onboarding, ensureProject200OnboardingSchema, getProject200Onboarding, initializeProject200Onboarding, markProject200OnboardingAvatarComplete, restartProject200Onboarding, saveProject200OnboardingProgress } from "./src/project200-onboarding.js";\nimport { listProject200LifeCaptures, patchProject200LifeCapture, upsertProject200LifeCapture } from "./src/project200-life-captures.js";\n', 'import life captures');
rep('async function handleProject200LifeCaptureUploadRequest(request, response) {\n', 'async function handleProject200LifeCaptureListRequest(request, response) {\n  const user = await requireAuth(request, response);\n  if (!user) return;\n  try {\n    const captures = await listProject200LifeCaptures(user.id);\n    sendJson(response, 200, { ok: true, captures });\n  } catch (error) {\n    sendJson(response, 400, { error: error instanceof Error ? error.message : "Nao foi possivel carregar as capturas." });\n  }\n}\n\nasync function handleProject200LifeCapturePatchRequest(request, response, captureId) {\n  const user = await requireAuth(request, response);\n  if (!user) return;\n  try {\n    const body = await readJsonBody(request);\n    const capture = await patchProject200LifeCapture(user.id, captureId, {\n      title: body?.title,\n      noteText: body?.noteText,\n      durationMs: body?.durationMs,\n      metadata: body?.metadata\n    });\n    if (!capture) {\n      sendJson(response, 404, { error: "Captura nao encontrada." });\n      return;\n    }\n    sendJson(response, 200, { ok: true, capture });\n  } catch (error) {\n    sendJson(response, 400, { error: error instanceof Error ? error.message : "Nao foi possivel atualizar a captura." });\n  }\n}\n\nasync function handleProject200LifeCaptureUploadRequest(request, response) {\n', 'insert handlers');
rep('    const kind = String(body?.kind || "photo").trim().toLowerCase() === "video" ? "video" : "photo";\n    const mimeType = String(body?.mimeType || "").trim().toLowerCase();\n    const fileBase64 = String(body?.fileBase64 || "").trim();\n    const previewBase64 = String(body?.previewBase64 || "").trim();\n', '    const kind = String(body?.kind || "photo").trim().toLowerCase() === "video" ? "video" : "photo";\n    const captureId = String(body?.captureId || body?.id || "").trim() || crypto.randomUUID();\n    const title = String(body?.title || "").trim();\n    const noteText = String(body?.noteText || "").trim();\n    const createdAt = body?.createdAt;\n    const durationMs = Math.max(0, Math.trunc(Number(body?.durationMs || 0) || 0));\n    const metadata = body?.metadata && typeof body.metadata === "object" ? body.metadata : {};\n    const mimeType = String(body?.mimeType || "").trim().toLowerCase();\n    const fileBase64 = String(body?.fileBase64 || "").trim();\n    const previewBase64 = String(body?.previewBase64 || "").trim();\n', 'upload parse');
rep('    sendJson(response, 201, {\n      ok: true,\n      asset: {\n        kind,\n        key: mediaKey,\n        url: buildPublicR2UrlFromKey(mediaKey),\n        previewKey,\n        previewUrl,\n        sizeBytes: mediaBuffer.length,\n        mimeType\n      }\n    });\n', '    const capture = await upsertProject200LifeCapture(user.id, {\n      id: captureId,\n      kind,\n      title,\n      noteText,\n      createdAt,\n      mimeType,\n      mediaKey,\n      mediaUrl: buildPublicR2UrlFromKey(mediaKey),\n      previewKey,\n      previewUrl,\n      sizeBytes: mediaBuffer.length,\n      durationMs,\n      metadata\n    });\n\n    sendJson(response, 201, {\n      ok: true,\n      asset: {\n        kind,\n        key: mediaKey,\n        url: buildPublicR2UrlFromKey(mediaKey),\n        previewKey,\n        previewUrl,\n        sizeBytes: mediaBuffer.length,\n        mimeType\n      },\n      capture\n    });\n', 'upload response');
rep('  if (request.method === "POST" && pathname === "/api/200/life-captures/upload") {\n    await handleProject200LifeCaptureUploadRequest(request, response);\n    return;\n  }\n', '  if (request.method === "GET" && pathname === "/api/200/life-captures") {\n    await handleProject200LifeCaptureListRequest(request, response);\n    return;\n  }\n\n  if (request.method === "PATCH" && pathname.match(/^\\/api\\/200\\/life-captures\\/[^/]+$/)) {\n    const captureId = decodeURIComponent(pathname.replace(/^\\/api\\/200\\/life-captures\\/([^/]+)$/, "$1"));\n    await handleProject200LifeCapturePatchRequest(request, response, captureId);\n    return;\n  }\n\n  if (request.method === "POST" && pathname === "/api/200/life-captures/upload") {\n    await handleProject200LifeCaptureUploadRequest(request, response);\n    return;\n  }\n', 'routes');
fs.writeFileSync(p, s, 'utf8');
console.log('server wired');
